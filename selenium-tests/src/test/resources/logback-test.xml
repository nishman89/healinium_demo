<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Suppress WebDriverManager download info -->
    <logger name="io.github.bonigarcia.wdm" level="WARN"/>

    <!-- Suppress Selenium CDP version warnings -->
    <logger name="org.openqa.selenium" level="WARN"/>

    <!-- Suppress Healenium HTTP queue noise -->
    <logger name="com.epam.healenium" level="WARN"/>

    <!-- Suppress OkHttp connection noise -->
    <logger name="okhttp3" level="WARN"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>